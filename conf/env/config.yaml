env:
  env_name: MiniGrid-Dynamic-Obstacles-8x8-v0 # Name of environment, or comma-separate list of environment names to instantiate as each env in the VecEnv.
  visualize: False # to choose whether your want to see the env during interaction or not
collect:
  episodes: 10000 # number of episodes to start 
  data_train: ${oc.env:TRAIN_DATASET_PATH}/gridworld_full.npz # folder where to save the rollouts
